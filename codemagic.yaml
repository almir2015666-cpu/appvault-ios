workflows:
  ios-release:
    name: AppVault - iOS App Store Release
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: appstore_codemagic_admin
    environment:
      groups:
        - appvault
      vars:
        BUNDLE_ID: com.appvault.app
        XCODE_SCHEME: AppVault
        APPLE_TEAM_ID: 66U94U6Q5H
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
    scripts:
      - name: Instalar XcodeGen
        script: brew install xcodegen

      - name: Gerar projeto Xcode
        script: xcodegen generate

      - name: Configurar keychain e certificados
        script: |
          keychain initialize
          mkdir -p ~/Library/MobileDevice/Certificates
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles

          # Instalar certificado de distribuição
          app-store-connect certificates create \
            --type IOS_DISTRIBUTION \
            --certificate-key "$DIST_CERT_KEY" \
            --p12-password "appvault2024" \
            --p12-path ~/Library/MobileDevice/Certificates/distribution.p12 || true

          PROFILES_DIR=~/Library/MobileDevice/Provisioning\ Profiles

          if [ -n "$PROVISIONING_PROFILE_BASE64" ]; then
            echo "Instalando provisioning profile manual..."
            echo "$PROVISIONING_PROFILE_BASE64" | base64 --decode > "$PROFILES_DIR/AppVault_AppStore.mobileprovision"
          else
            # Ativar capabilities no bundle ID antes de criar o profile
            BUNDLE_RES_ID=$(app-store-connect bundle-ids list \
              --bundle-id-identifier "$BUNDLE_ID" \
              --json 2>/dev/null | \
              python3 -c "import json,sys; d=json.load(sys.stdin); print(d[0]['id'] if d else '')" 2>/dev/null || echo "")

            if [ -n "$BUNDLE_RES_ID" ]; then
              echo "Bundle ID resource: $BUNDLE_RES_ID"
              echo "Ativando FAMILY_CONTROLS..."
              app-store-connect bundle-ids enable-capability \
                --bundle-id-resource-id "$BUNDLE_RES_ID" \
                --capability-type FAMILY_CONTROLS 2>&1 || true
              echo "Ativando APP_GROUPS..."
              app-store-connect bundle-ids enable-capability \
                --bundle-id-resource-id "$BUNDLE_RES_ID" \
                --capability-type APP_GROUPS 2>&1 || true
            fi

            # Deletar todos os profiles existentes e criar um novo
            for pid in $(app-store-connect profiles list \
              --bundle-id-identifier "$BUNDLE_ID" \
              --profile-type IOS_APP_STORE \
              --json 2>/dev/null | \
              python3 -c "import json,sys; [print(x['id']) for x in json.load(sys.stdin)]" 2>/dev/null || true); do
              echo "Deletando perfil $pid"
              app-store-connect profiles delete "$pid" 2>/dev/null || true
            done

            app-store-connect fetch-signing-files "$BUNDLE_ID" \
              --type IOS_APP_STORE --create \
              --certificate-key "$DIST_CERT_KEY" \
              --p12-password "appvault2024" \
              --certificates-dir ~/Library/MobileDevice/Certificates/
          fi

          # Verificar e mostrar entitlements do profile resultante
          echo "=== ENTITLEMENTS DO PROFILE ==="
          for f in "$PROFILES_DIR"/*.mobileprovision; do
            [ -f "$f" ] || continue
            security cms -D -i "$f" 2>/dev/null | python3 -c "import plistlib,sys; d=plistlib.loads(sys.stdin.buffer.read()); ents=d.get('Entitlements',{}); fc='com.apple.developer.family-controls'; print('family-controls no profile:', fc in ents); [print(' ',k) for k in sorted(ents.keys())]"
          done

          keychain add-certificates --certificate-password "appvault2024"

      - name: Configurar assinatura
        script: xcode-project use-profiles

      - name: Número de build único
        script: agvtool new-version -all $(date +%s)

      - name: Build e Archive
        script: |
          xcodebuild \
            -project AppVault.xcodeproj \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -archivePath build/AppVault.xcarchive \
            -destination "generic/platform=iOS" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            archive

      - name: Exportar .ipa
        script: |
          python3 - << 'PYEOF'
          import subprocess, plistlib, glob, os, sys

          provisioning = {}
          for d in [
            "~/Library/MobileDevice/Provisioning Profiles",
            "~/Library/Developer/Xcode/UserData/Provisioning Profiles"
          ]:
            for f in glob.glob(os.path.expanduser(d + "/*.mobileprovision")):
              r = subprocess.run(["security", "cms", "-D", "-i", f], capture_output=True)
              if r.returncode != 0:
                continue
              pl = plistlib.loads(r.stdout)
              bid = pl.get("Entitlements", {}).get("application-identifier", "")
              bid = ".".join(bid.split(".")[1:]) if "." in bid else ""
              name = pl.get("Name", "")
              if bid and name and "appvault" in bid.lower():
                provisioning[bid] = name
                print(f"  {bid} -> {name}", flush=True)

          if not provisioning:
            print("ERRO: nenhum perfil encontrado!", file=sys.stderr)
            sys.exit(1)

          opts = {
            "method": "app-store-connect",
            "teamID": "66U94U6Q5H",
            "signingStyle": "manual",
            "stripSwiftSymbols": True,
            "uploadSymbols": True,
            "provisioningProfiles": provisioning
          }
          with open("ExportOptions.plist", "wb") as fh:
            plistlib.dump(opts, fh)
          print(f"ExportOptions.plist gerado com {len(provisioning)} perfis", flush=True)
          PYEOF

          xcodebuild \
            -exportArchive \
            -archivePath build/AppVault.xcarchive \
            -exportPath build/export \
            -exportOptionsPlist ExportOptions.plist

    artifacts:
      - build/export/*.ipa
      - build/AppVault.xcarchive

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true

  ios-simulator:
    name: AppVault - Validar Build (Simulador)
    max_build_duration: 20
    instance_type: mac_mini_m2
    scripts:
      - name: Instalar XcodeGen
        script: brew install xcodegen

      - name: Gerar projeto Xcode
        script: xcodegen generate

      - name: Build para Simulador
        script: |
          xcodebuild build \
            -project AppVault.xcodeproj \
            -scheme AppVault \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO
